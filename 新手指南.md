# 新手指南（不需要懂技术）

这份说明假设你完全不会编程、没用过「终端」。按顺序做就行。

---

## 第一部分：你的 Mac

### 1. 下载

打开这个地址：https://github.com/taotao-river/-

点绿色的 **「Code」** 按钮 → **「Download ZIP」**。

下载完在「下载」文件夹里找到那个 zip，**双击解压**。会得到一个文件夹。

### 2. 双击安装

打开那个文件夹，找到 **「安装到Mac.command」**。

> ⚠️ **第一次一定要右键点它 → 选「打开」**，不能直接双击。
> 因为是从网上下载的，Mac 会拦一下。右键打开时会弹窗问「确定要打开吗」，
> 点「打开」就行。之后再用就可以直接双击了。

会弹出一个黑色窗口，按提示一路按键就行。中途可能要你输 Mac 的开机密码。

装完它会自动打开一个设置页面，让你**给「终端」打开辅助功能开关**。
这一步不做的话，程序读不到微信消息。

> 如果那个列表里找不到「终端」：点列表下面的 **➕**，
> 然后从「应用程序 → 实用工具」里选「终端」。

### 2.4 回答十个问题

装到一半它会停下来问你十个问题，像这样：

```
  ── 第 3 题 / 共 10 题 ──

  你平时打字是什么感觉？
  （这一题最影响像不像你本人）
    1. 简短随便，跟熟人聊天那种
    2. 客气一点，会用「您」
    3. 热情，愿意多聊两句，爱开玩笑
    4. 能少说就少说，一两个字就完事
  > 1
```

**打数字按回车就行**，八道是选择题，一分钟能答完。两道填空题
（「别人发『在吗』你自己会怎么回」和「有什么绝对不能替你答应」）
直接回车就能跳过。

答完它会先把生成的话摆出来给你看：

```
  别人说「在吗」        → 在，怎么了
  别人约你吃饭          → 我看下日程，晚点回你
  别人问事情办得怎样    → 在弄了，这两天给你结果
  别人推销、拉群        → 这个我不太需要，谢谢
  其他都没匹配上        → 我白天要上班，手机不太方便看，看到会尽快回你
```

不满意就输 `n` 重答一遍。满意直接回车。

> 为什么要问这些：这套系统最难写的就是「你是个什么样的人」。
> 与其让你对着一个空框发愁，不如问几个你张口就能答的问题，
> 它自己把话组织好。

### 2.5 先看看会回什么（重要）

在真正开始之前，先把文案调顺——你和你朋友的联系人会看到这些话。

打开「终端」，进到文件夹里执行：

```bash
python3 -m core.preview
```

然后直接打字模拟别人发消息，看它会回什么：

```
对方说> 在吗
  ✅ 会回复：在的，我这会儿在忙，稍后回你（自动回复）

对方说> 帮我转账500
  ⛔ 不会回复：命中硬性敏感词 '转账'，人工处理
```

觉得话说得不合适，有两个办法：

- 重新答一遍问答：`python3 -m core.wizard`
- 或者直接改 `core/config.yaml` 里的文案

改完在预览里输入 `/reload` 重新加载，接着试。反复调到满意为止。

**这一步不会发任何消息**，也不占用「每天最多回几条」的额度。

### 3. 先试运行

回到文件夹，双击 **「试运行（不真发消息）.command」**。

**这一步只是看看，不会真的发消息出去。**

你要在窗口里找到类似这样的内容：

```
「小王」最后一条: 在吗
  [DRY-RUN] 本应回复: 在的，我这会儿在忙，稍后回你（自动回复）
```

- **如果会话名和消息内容都是对的** → 成功了，继续第 4 步
- **如果显示「找不到会话列表，可能是微信版本变了」** → 把整个窗口截图，
  发给帮你配置的人。这是最常见的问题，需要改一下代码才能适配你的微信版本
- **如果显示「未授予辅助功能权限」** → 回第 2 步最后那个授权没做好

关掉窗口就停了。

### 4. 正式开始

确认第 3 步没问题后，双击 **「开始自动回复.command」**。

这个窗口**得一直开着**，关掉就停了。可以最小化。

**建议再做一件事**：系统设置 → 电池 → 把「睡眠」设成「永不」。
否则合上盖子自动回复就停了。

### 5. 改回复内容

**最省事的办法：重新答一遍问答。** 在终端里执行：

```bash
python3 -m core.wizard
```

它会把整套话重新生成一遍（原来的配置会自动备份成
`core/config.yaml.bak`，改坏了可以拿回来）。

想只改某一句的话，用「文本编辑」打开 `core/config.yaml`。
里面中文注释写得很清楚，你主要改这几处：

```yaml
rules:
  - name: 打招呼
    match:
      type: keyword
      any: ["在吗", "在么", "忙吗"]      # 收到这些词时
    reply:
      - "在的，我这会儿在忙，稍后回你"    # 就回这句
      - "在，手上有点事，等下详细说"      # 或这句（轮流用）
```

改完保存，然后**关掉「开始自动回复」窗口再双击打开一次**就生效了。

> 改的时候注意：不要动每行前面的空格数量，这个格式对空格很敏感。

---

## 第二部分：你朋友的安卓

### 1. 拿 APK

APK 不用自己编译，GitHub 会自动编好：

打开 https://github.com/taotao-river/-/actions →
点最上面那条绿色✅的记录 → 页面最下面 **Artifacts** →
下载 **「微信自动回复-apk」**。

解压出来是个 `.apk` 文件，用微信发给你朋友就行。

> 如果 Actions 页面是空的或者是红色❌，说明还没编译成功，
> 把页面截图发给帮你配置的人。

### 2. 他手机上安装

他点开你发的 apk 安装。中途会提示「禁止安装未知应用」，
按提示去设置里允许「微信」（或者他用的文件管理器）安装应用就行。

### 2.2 他第一次打开会先答十道题

装完打开，App 会直接进一个问答页，一屏一题：

> **你平时打字是什么感觉？**
> ○ 简短随便，跟熟人聊天那种
> ○ 客气一点，会用「您」
> ○ 热情，愿意多聊两句，爱开玩笑
> ○ 能少说就少说，一两个字就完事

**点一下就跳下一题**，十道题里八道是这样的单选，全程基本不用打字。
两道填空可以点「这题跳过」。

答完会先把生成的话摆出来给他看，点「就用这套」才会存下来。
不满意可以点「重新答一遍」。

**让他自己答，别替他答**——这一步决定了回复像不像他本人。
以后想改，设置页里有个「回答几个问题，自动生成」可以随时重来。

### 2.5 选「怎么回」：关键词，还是 AI

App 里有一栏 **「怎么回」**，两个选项：

**① 按关键词回固定的话**（默认，装完就能用）

事先写好「收到『在吗』就回『在的，稍后回你』」。不联网、不花钱、
不需要注册任何东西。缺点也很明显：只能回你想到的那几句，
别人说点别的就只能回兜底那一句——这跟当年 QQ 的自动回复是一个东西。

**② 让 AI 按你的说话方式现写**

每条消息现写一句，看得懂对方在说什么。你不用去猜别人会发什么，
只要在下面填清楚**你是个什么样的人、各种情况怎么应对**，AI 会照着判断。

选了 AI 之后，下面还要二选一：

- **「我自己注册一个」（推荐）** —— 去 DeepSeek / 通义千问 / 智谱 /
  Moonshot 任意一家的官网注册，在「API Key」页面点新建，把那串字符
  粘进 App。国内直接能用，不用翻墙；多数家新注册都送额度，
  自动回复用量很小基本花不到钱。
  App 里点一下那家的按钮，接口地址和模型名会自动填好，他只需要粘 key。
  这条路他**谁也不依赖**，你那边关不关机都不影响他。

- **「用别人给我的地址」** —— 你在自己电脑上跑起本仓库的
  Python 服务（见 `docs/deployment.md`），把地址和口令发给他，
  他粘进 App 就行，不用注册任何账号。
  代价是：他的消息会经过你的服务器，回复也由你那边生成，
  而且你电脑关机他就不回了。**只推荐在你俩都清楚这一点的前提下用。**

> 两个选项随时可以换，换完点「保存设置」就生效。

### 2.6 让他先「试一试」

App 里最下面有个 **「试一试（不会真的发出去）」**：输入一句话，
点「看看会回什么」，就能看到会回什么、或者为什么不回。

**让他在打开总开关之前先试几句**，把语气调成他自己的。
改完点「保存设置」。试的时候不会发消息，也不占用每日额度。

选了 AI 模式的话，这里会**真的调一次接口**，所以顺便也验证了
key 有没有填对——如果 key 是错的，这里就会告诉他。

### 3. 授权 + 打开开关

App 里点 **「授予通知使用权（必须）」**，在跳转的系统页面里找到
「微信自动回复」并打开。不给这个权限，程序看不到微信消息。

回到 App，把最上面那个 **大开关拨到开**。上面会显示「✅ 正在工作中」。

**就这样，完事了。**

用关键词模式的话，到此为止不需要电脑、不需要连你的 Wi-Fi、不需要任何服务器。
用 AI 模式的话，手机需要能上网，但仍然不需要电脑（除非他选的是「用别人给我的地址」，
那就得你那边的服务开着）。

### 4. 教他怎么随时开关

自动回复不是一直要开着的。两个办法关掉：

- 打开 App 把大开关拨回去
- 或者：下拉通知栏 → 点编辑（铅笔图标）→ 把「微信自动回复」
  拖进快捷开关。以后下拉点一下就切换，跟开关手电筒一样

> 已经排队等着发的消息，在开关关掉后也不会发出去。

### 5. 让他注意省电限制

有些手机（小米、华为、OPPO 等）会自动杀后台。让他去设置里
把「微信自动回复」和「微信」都加进省电白名单，否则可能过一阵就不工作了。

---

## 常见问题

**Q：会不会被封号？**
有风险。程序已经做了防护（不秒回、同一个人半小时内只回一次、
每天最多回 5 条、遇到转账红包验证码这类消息绝不自动回），
但**不能保证一定没事**。

建议一开始只对两三个熟人开启，观察几天再放开。

**Q：对方知道是机器人回的吗？**
默认每条回复末尾会带「（自动回复）」。建议保留，免得引起误会。

**Q：转账、红包会不会被自动回复？**
不会。这类消息程序直接跳过交给你本人处理，这个行为改不了。

**Q：怎么临时停掉？**
- Mac：关掉「开始自动回复」那个窗口
- 安卓：App 里把大开关拨回去，或者用下拉通知栏的快捷开关

**Q：重启后还要重新弄吗？**
- Mac：后台服务会自动启动，但「开始自动回复」窗口要重新双击一次
- 安卓：不用管，开关状态会记住

**Q：安卓那边要不要电脑？**
不要。关键词模式下所有判断都在手机里完成，不联网、不上传消息、也不连你的电脑。
选了 AI 模式且用「自己注册的 key」，手机联网就行，还是不需要电脑。
只有选「用别人给我的地址」时，才依赖对方那台电脑开着。

**Q：AI 模式会把我的聊天记录传出去吗？**
会把**需要回复的那条消息**和最近几轮上下文发给你选的那家接口，
否则它没法知道该回什么。要注意的是：

- 含转账、红包、验证码、借钱这类字样的消息**根本不会发出去**，
  程序在本机就拦掉了，压根轮不到模型
- 黑名单里的人、按策略不回的群消息，也都在本机就拦掉了，不会发出去
- 上下文只存在手机内存里，最多留 8 轮、1 小时，App 一关就没了，不落盘

介意的话就用关键词模式，那个模式一个字节都不往外发。

**Q：AI 会不会乱答应事情？**
提示词里硬性写了不承诺金额时间地点、不答应转账借钱代付、不自称 AI。
但这些是**提示词**，是概率性的，不是保险。真正的保险是本机那几条：
敏感词、黑名单、群聊策略、频率限制——这几条在消息发给模型之前就执行完了，
模型只决定「说什么」，不决定「该不该说」。

所以还是那句话：一开始只对两三个熟人开启，观察几天。

**Q：AI 模式要花钱吗？**
看你选的那家怎么收费。自动回复的用量很小（一条消息几百个字符），
多数家新注册送的额度够用很久。
另外程序本身有限流：同一个人半小时内只回一次、每天最多几条，
不会出现狂刷接口的情况。接口调用失败时程序直接不回，也不会重试烧钱。

**Q：我改坏了配置文件怎么办？**
跑 `python3 -m core.wizard` 重新答一遍问答，它会重新生成一份完整的。
（每次生成前都会把旧的备份成 `core/config.yaml.bak`。）

**Q：问答生成的话我不太满意，是不是就只能用这几句？**
不是。那几句是**示范**：

- 开了 AI 模式的话，AI 是照着这个语气自己判断该说什么的，
  不是只会回这几句——别人说什么它都能接
- 用关键词模式的话，确实只有那几句，但你可以在配置文件
  （或安卓的设置页）里随便加

另外第 9 题「别人发『在吗』你自己会怎么回」建议一定要自己填，
用你平时真会说的话。你自己的一句话，比它按风格挑的任何一句都准。

---

## 需要帮忙的时候

出问题时，把这些发给帮你配置的人：

1. 出错窗口的**完整截图**
2. 如果是 Mac：文件夹里 `var/server.err.log` 这个文件
3. 你的微信版本号（微信 → 设置 → 关于微信）

微信版本号很重要——这个程序读微信界面的方式跟版本有关，
换个版本可能就要改一下。
